version: "3.7"

services:
  Lumos:
    image: docker.io/mduchalski/lumos
    ports:
      - 10001:443
    environment:
      - DB_SERVER=mysql # Change for deployment
      - DB_NAME=Lumos # Review if change needed
      - DB_USER=Lumos # Review if change needed
      - DB_PASSWD=lampexpol
      - PS_INSTALL_AUTO=1
      - PS_DOMAIN=localhost:10001 # Change for depoloyment
      - PS_LANGUAGE=pl
      - PS_COUNTRY=PL
      - PS_FOLDER_ADMIN=admin1
      - PS_ENABLE_SSL=1
    networks: # Remove for deployment
      - prestashop-net

networks: # Remove for deployment
  prestashop-net:
    external: True
